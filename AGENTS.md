# AGENTS.md

本仓库的全部协作规则、架构说明与工作流程见 [CLAUDE.md](CLAUDE.md)(**单源维护**,本文件只做重定向,不复制内容)。

Codex 等其他 agent 同样遵循 CLAUDE.md。**特别注意:仓库里的真实路径是 `.claude-plugin/` 和环境变量 `${CLAUDE_PLUGIN_ROOT}`——这些是磁盘上固定的名字,不随读它的 agent 改变,不要替换成 `.Codex-plugin/` 之类的坏路径。**
